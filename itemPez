
public class itemPez extends item {

    public itemPez() {
        super("Pez", "Soborna al oso para evitar volver al inicio. También sirve para bloquear a la foca 2 turnos.");
    }

    @Override
    public void usar(jugador jugador) {
        System.out.println(jugador.getNombre() + " usa un pez.");
    }
}
